<h1>{{title}}</h1>
<p>Welcome to {{title}}
  {{#if currentUser}}
  , {{currentUser.first_name}}
  {{/if}}
</p>

<h2>Poems</h1>
{{#if poems}}
  {{#each poems}}
    {{this}}
  {{/each}}
{{/if}}

{{#if currentUser}}
<a href="/logout">
Logout
</a>
{{/if}}